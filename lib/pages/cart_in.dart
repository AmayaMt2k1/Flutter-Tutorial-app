import 'package:flutter/material.dart';

class CartIn extends StatelessWidget {
  const CartIn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Your Cart'),
      ),
    );
  }
}
